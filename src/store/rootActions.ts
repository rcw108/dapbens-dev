import {
	pushAllProducts,
	pushCategories,
	pushTags,
	setPopularCategories
} from './products/product.slice'

export const allActions = {
	pushAllProducts,
	setPopularCategories,
	pushTags,
	pushCategories
}
